# TODO

- [ ] Replace GitHub auto-generated release notes with a repo-owned workflow that extracts the released changelog section, adds verification proof, and passes that body to `gh release create --notes-file`.
